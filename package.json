{
  "name": "@scicave/math-latex-parser",
  "version": "1.0.0",
  "description": "A mathematical parser for latex in math mode. We mean by mathematical that, arithmetic operations is considered for example if you pass \"1+2\", the result would by a (add node \"+\") with two children nodes of type number.",
  "main": "./lib/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/scicave/math-latex-parser.git"
  },
  "bugs": {
    "url": "https://github.com/scicave/math-latex-parser/issues"
  },
  "homepage": "https://github.com/scicave/math-latex-parser#readme",
  "scripts": {
    "prebuild": "node ./tools/generate-version-file.js",
    "build": "node build.js",
    "buildtest": "npm run build && npm run test",
    "test": "jest ./src/__tests__/parser.test.js",
    "lint": "eslint ./src/ --fix",
    "prod": "npm run lint && npm run build && npm run test",
    "dev": "nodemon --watch src --watch tools --watch build.js --ignore src/version.js --exec \"npm run buildtest\"",
    "debug": "node debug.js",
    "build:watch": "nodemon --watch src --watch tools --watch build.js --ignore src/__tests__ --ignore src/version.js --exec \"npm run build\"",
    "debug:watch": "nodemon --watch src --watch tools --watch build.js --watch debug.js --ignore src/__tests__ --ignore src/version.js --exec \"npm run debug\"",
    "test:watch": "jest ./src/__tests__/parser.test.js --watch",
    "preversion": "npm run prod"
  },
  "keywords": [
    "parser",
    "pegjs",
    "latex-parser",
    "math-parser",
    "math",
    "mathematics"
  ],
  "author": "scicave",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^7.8.1",
    "jest": "^26.4.2",
    "nodemon": "^2.0.4",
    "pegjs": "^0.10.0",
    "rimraf": "^3.0.2"
  }
}
